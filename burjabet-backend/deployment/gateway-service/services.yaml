apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: burja
spec:
  type: ClusterIP
  selector:
    app: gateway-service
  ports:
  - name: http
    port: 80
    targetPort: 3004
    protocol: TCP
