apiVersion: v1
kind: Service
metadata:
  name: blockchain-service
  namespace: burja
spec:
  type: ClusterIP
  selector:
    app: blockchain-service
  ports:
    - name: http
      port: 3002
      targetPort: 3002
      protocol: TCP


